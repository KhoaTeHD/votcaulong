jQuery(document).ready(function(o){translations.add_product;compareList=JSON.parse(localStorage.getItem("compareList"))||[];let t=[];compareList?compareList.forEach(o=>{t.push(o.sku)}):t=getSkusFromUrl(),t.length>0&&loadProductComparison(t)});